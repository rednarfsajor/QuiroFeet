@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Consultar Agenda";
}

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-light">
            <h2 class="mb-0 h5"><i class="fas fa-book-medical me-2"></i> @ViewBag.Title</h2>
        </div>
        <div class="card-body">
            <p>Mostrando agenda del empleado con ID: <strong>@Request.QueryString["id"]</strong></p>

            <div class="alert alert-info">
                Esta es una vista simulada de la agenda del empleado. Aquí podrías mostrar citas, horarios disponibles, etc.
            </div>

            <ul class="list-group">
                <li class="list-group-item">🗓️ Lunes - 09:00 a 12:00</li>
                <li class="list-group-item">🗓️ Miércoles - 14:00 a 18:00</li>
                <li class="list-group-item">🗓️ Viernes - 10:00 a 13:00</li>
            </ul>

            <a href="@Url.Action("Index", "Profesionales")" class="btn btn-secondary btn-sm mt-3">
                <i class="fas fa-arrow-left me-1"></i> Volver a la lista
            </a>
        </div>
    </div>
</div>
