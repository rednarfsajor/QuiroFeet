@model IEnumerable<Analisis.BD.Usuarios>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Usuarios Inactivos";
}

<div class="user-container">
    <h1><i class="bi bi-person-dash-fill"></i> Usuarios Inactivos</h1>
    <p>Usuarios inactivos actualmente en el sistema.</p>

    @if (TempData["ActivateSuccess"] != null)
    {
        <div class="success-message">
            <i class="bi bi-check-circle-fill"></i> @TempData["ActivateSuccess"]
        </div>
    }

    @if (Model != null && Model.Any())
    {
        <table class="user-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre Completo</th>
                    <th>Correo Electrónico</th>
                    <th>Rol</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nombre</td>
                        <td>@item.Email</td>
                        <td>@(item.Roles != null ? item.Roles.Nombre : "Sin Rol")</td>
                        <td>
                            @using (Html.BeginForm("ActivarUser", "Usuarios", FormMethod.Post, new { @class = "inline-form" }))
                            {
                                @Html.AntiForgeryToken()
                                @Html.Hidden("id", item.Id)
                                <button type="submit" class="btn btn-activate">
                                    <i class="bi bi-person-check-fill"></i> Activar
                                </button>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="no-data-message">
            <i class="bi bi-exclamation-triangle-fill"></i>
            No hay usuarios inactivos en el sistema.
        </div>
    }
</div>

<!-- Estilos CSS -->
<style>
    body {
        font-family: 'Poppins', sans-serif;
    }

    .user-container {
        max-width: 950px;
        margin: 60px auto;
        background: #ffffff;
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    h1 {
        color: #234449;
        font-size: 2em;
        margin-bottom: 10px;
    }

    p {
        font-size: 1.1em;
        color: #555;
        margin-bottom: 30px;
    }

    .user-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 0.95em;
    }

        .user-table th, .user-table td {
            border: 1px solid #ccc;
            padding: 12px;
            text-align: center;
        }

        .user-table th {
            background-color: #234449;
            color: white;
        }

        .user-table tbody tr:hover {
            background-color: #f4f9ff;
        }

    .btn {
        padding: 8px 14px;
        border-radius: 6px;
        font-size: 0.9em;
        text-decoration: none;
        color: white;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        margin: 2px;
        border: none;
        cursor: pointer;
    }

    .btn-activate {
        background-color: #28a745;
    }

        .btn-activate:hover {
            background-color: #218838;
        }

    .success-message {
        background-color: #e6f4ea;
        color: #155724;
        border: 1px solid #c3e6cb;
        border-radius: 10px;
        padding: 14px 20px;
        font-size: 1em;
        font-weight: 600;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        margin-bottom: 20px;
    }

        .success-message i {
            font-size: 1.2em;
            color: #28a745;
        }

    .no-data-message {
        margin-top: 30px;
        background-color: #fff3f3;
        color: #a94442;
        border: 1px solid #f5c6cb;
        border-radius: 10px;
        padding: 16px 24px;
        font-size: 1em;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        font-weight: 600;
    }

        .no-data-message i {
            font-size: 1.3em;
            color: #e74c3c;
        }
</style>
